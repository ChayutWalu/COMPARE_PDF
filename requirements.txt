# PDF Compare - Requirements
# Compatible with macOS, Windows, and Linux

# OCR Engine
# Typhoon OCR 1.5 รันผ่าน Ollama (LOCAL) - ไม่ต้อง pip install
# ติดตั้ง Ollama แล้วรัน: ollama pull scb10x/typhoon-ocr1.5-3b

# EasyOCR - สำหรับ bounding box extraction (visual highlighting)
easyocr>=1.7.0

# PDF Processing
PyMuPDF>=1.23.0

# Image Processing
Pillow>=10.0.0
numpy>=1.24.0
opencv-python-headless>=4.8.0

# Machine Learning (PyTorch)
# For macOS (Intel/Apple Silicon) - use CPU version
# For Windows/Linux with NVIDIA GPU - can use CUDA version
torch>=2.0.0
torchvision>=0.15.0

# GUI
customtkinter>=5.2.0

# Database (Supabase)
supabase>=2.0.0

# LLM API
openai>=1.0.0

# Environment Variables
python-dotenv>=1.0.0

# PDF Generation
reportlab>=4.0.0

# Utilities
tqdm>=4.65.0
